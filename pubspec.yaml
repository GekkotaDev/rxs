name: rxs
description: Another simple reactive state management library for Dart and Flutter applications.
version: 2.0.0
repository: https://github.com/gekkotadev/rxs.git

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  meta: ^1.9.1
  rxdart: ^0.27.7
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
